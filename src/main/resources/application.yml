server:
  port: 8080
  servlet:
    context-path: commmunity
spring:
  thymeleaf:
    #关闭thymeleaf缓存
    cache: false
  #MySQL数据库连接池配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/community?characterEncoding=utf-8&useSSL=false&serverTimezone=Hongkong
    username: root
    password: root